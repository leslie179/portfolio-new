import styles from './ProjectBlockName.module.css';

const ProjectBlockName = () => {
    return (
        <div styles={}>
        <h1>Projects</h1>
        </div>
    );
};

export default ProjectBlockName;
